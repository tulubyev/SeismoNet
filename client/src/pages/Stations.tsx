import { FC } from 'react';
import StationList from '@/components/stations/StationList';

const Stations: FC = () => (
  <div className="p-6">
    <StationList />
  </div>
);

export default Stations;
